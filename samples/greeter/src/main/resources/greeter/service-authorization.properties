# Service and Actions
service=greeter
actions=greet

# Service Policies
policy.read-only.allow=greet

# Service Roles
role.admin.policies=serviceaccess
role.viewer.policies=read-only

# Default Groups
group.admins.roles=admin
group.viewers.roles=viewer
